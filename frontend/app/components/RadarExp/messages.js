/*
 * RadarExp Messages
 *
 * This contains all the text for the RadarExp component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.RadarExp";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the RadarExp component!"
  }
});
