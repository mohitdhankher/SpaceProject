/*
 * NavigationTabs Messages
 *
 * This contains all the text for the NavigationTabs component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.NavigationTabs";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the NavigationTabs component!"
  }
});
