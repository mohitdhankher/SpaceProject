/*
 * Chartjs Messages
 *
 * This contains all the text for the Chartjs component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.Chartjs";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the Chartjs component!"
  }
});
