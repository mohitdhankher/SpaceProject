/*
 * Chart Messages
 *
 * This contains all the text for the Chart component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.Chart";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the Chart component!"
  }
});
