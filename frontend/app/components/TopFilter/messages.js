/*
 * TopFilter Messages
 *
 * This contains all the text for the TopFilter component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.TopFilter";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the TopFilter component!"
  }
});
