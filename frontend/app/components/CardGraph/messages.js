/*
 * CardGraph Messages
 *
 * This contains all the text for the CardGraph component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.CardGraph";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the CardGraph component!"
  }
});
