/*
 * NavLinks Messages
 *
 * This contains all the text for the NavLinks component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.NavLinks";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the NavLinks component!"
  }
});
