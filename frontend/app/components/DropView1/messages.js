/*
 * DropView1 Messages
 *
 * This contains all the text for the DropView1 component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.DropView1";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the DropView1 component!"
  }
});
