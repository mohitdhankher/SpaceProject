/*
 * HomeNavBar Messages
 *
 * This contains all the text for the HomeNavBar component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.HomeNavBar";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the HomeNavBar component!"
  }
});
