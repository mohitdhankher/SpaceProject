/*
 * RoiRevenueGraph Messages
 *
 * This contains all the text for the RoiRevenueGraph component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.RoiRevenueGraph";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the RoiRevenueGraph component!"
  }
});
