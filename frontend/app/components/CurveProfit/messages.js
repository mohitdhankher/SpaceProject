/*
 * CurveProfit Messages
 *
 * This contains all the text for the CurveProfit component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.CurveProfit";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the CurveProfit component!"
  }
});
