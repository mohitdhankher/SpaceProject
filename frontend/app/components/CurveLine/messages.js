/*
 * CurveLine Messages
 *
 * This contains all the text for the CurveLine component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.CurveLine";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the CurveLine component!"
  }
});
