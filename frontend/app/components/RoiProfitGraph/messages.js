/*
 * RoiProfitGraph Messages
 *
 * This contains all the text for the RoiProfitGraph component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.RoiProfitGraph";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the RoiProfitGraph component!"
  }
});
