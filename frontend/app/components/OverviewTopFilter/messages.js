/*
 * OverviewTopFilter Messages
 *
 * This contains all the text for the OverviewTopFilter component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.OverviewTopFilter";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the OverviewTopFilter component!"
  }
});
