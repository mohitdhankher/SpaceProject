/*
 * ControlPanel Messages
 *
 * This contains all the text for the ControlPanel component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.ControlPanel";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the ControlPanel component!"
  }
});
