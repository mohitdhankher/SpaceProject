/*
 * RiskCharts Messages
 *
 * This contains all the text for the RiskCharts component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.RiskCharts";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the RiskCharts component!"
  }
});
