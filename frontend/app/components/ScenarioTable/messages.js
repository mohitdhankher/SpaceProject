/*
 * ScenarioTable Messages
 *
 * This contains all the text for the ScenarioTable component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.ScenarioTable";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the ScenarioTable component!",
    data: [
      
    ]
  }
});
