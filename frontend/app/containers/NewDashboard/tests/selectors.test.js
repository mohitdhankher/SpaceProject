// import { selectNewDashboardDomain } from '../selectors';

describe("selectNewDashboardDomain", () => {
  it("Expect to have unit tests specified", () => {
    expect(true).toEqual(false);
  });
});
