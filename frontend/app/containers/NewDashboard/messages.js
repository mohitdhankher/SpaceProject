/*
 * NewDashboard Messages
 *
 * This contains all the text for the NewDashboard container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.NewDashboard";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the NewDashboard container!"
  }
});
