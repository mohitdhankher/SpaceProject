/*
 * Graphs Messages
 *
 * This contains all the text for the Graphs container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.Graphs";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the Graphs container!"
  }
});
