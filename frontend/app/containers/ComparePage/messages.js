/*
 * ComparePage Messages
 *
 * This contains all the text for the ComparePage container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.ComparePage";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the ComparePage container!"
  }
});
