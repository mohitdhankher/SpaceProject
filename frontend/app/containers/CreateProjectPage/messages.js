/*
 * CreateProjectPage Messages
 *
 * This contains all the text for the CreateProjectPage container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.CreateProjectPage";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the CreateProjectPage container!"
  }
});
