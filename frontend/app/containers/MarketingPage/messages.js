/*
 * MarketingPage Messages
 *
 * This contains all the text for the MarketingPage container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.MarketingPage";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the MarketingPage container!"
  }
});
