/*
 * SimulationPage Messages
 *
 * This contains all the text for the SimulationPage container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.SimulationPage";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the SimulationPage container!"
  }
});
