/*
 * ScenarioList Messages
 *
 * This contains all the text for the ScenarioList container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.ScenarioList";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the ScenarioList container!"
  }
});
