// import { selectScenarioListDomain } from '../selectors';

describe("selectScenarioListDomain", () => {
  it("Expect to have unit tests specified", () => {
    expect(true).toEqual(false);
  });
});
