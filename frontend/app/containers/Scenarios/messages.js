/*
 * Scenarios Messages
 *
 * This contains all the text for the Scenarios container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.Scenarios";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the Scenarios container!"
  }
});
